# eslint-config-next pulls its plugins (react-hooks, jsx-a11y, import, etc.)
# in as transitive deps. ESLint's FlatCompat resolves them from the project
# root, so hoist anything eslint-related out of pnpm's isolated store.
public-hoist-pattern[]=*eslint*
