import { ComingSoon } from "@/components/shared/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Export and import your data as JSON, or reset and reseed the demo."
      milestone="Milestone 7"
    />
  );
}
